import React, { useState }from 'react';

const QuestionForm = () => {
  const [body, setBody] = useState('');
  return (
    <div>
      question form
    </div>
  );
};

export default QuestionForm;
